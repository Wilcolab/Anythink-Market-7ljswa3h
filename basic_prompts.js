/**
 * Converts a string to camelCase format.
 * 
 * This function takes a string with spaces, hyphens, or underscores and converts it to camelCase.
 * The first letter is lowercase, and the first letter of each subsequent word is uppercase.
 * 
 * @param {string} str - The input string to convert
 * @returns {string} The camelCase version of the input string
 * 
 * @example
 * toCamelCase("hello world") // returns "helloWorld"
 * toCamelCase("convert-this-string") // returns "convertThisString"
 * toCamelCase("another_test_string") // returns "anotherTestString"
 * toCamelCase("Mixed-Style_String here") // returns "mixedStyleStringHere"
 */
function toCamelCase(str) {
    // Implementation goes here
}